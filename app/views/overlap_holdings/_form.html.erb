<%= form_for(@overlap_holding) do |f| %>
  <% if @overlap_holding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@overlap_holding.errors.count, "error") %> prohibited this overlap_holding from being saved:</h2>

      <ul>
      <% @overlap_holding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :oclc_number %><br>
    <%= f.number_field :oclc_number %>
  </div>
  <div class="field">
    <%= f.label :shared_by %><br>
    <%= f.number_field :shared_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
