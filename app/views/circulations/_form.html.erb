<%= form_for(@circulation) do |f| %>
  <% if @circulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@circulation.errors.count, "error") %> prohibited this circulation from being saved:</h2>

      <ul>
      <% @circulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :oclc_number %><br>
    <%= f.number_field :oclc_number %>
  </div>
  <div class="field">
    <%= f.label :loan_date %><br>
    <%= f.date_select :loan_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
