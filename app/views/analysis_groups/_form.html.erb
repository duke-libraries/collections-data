<%= form_for(@analysis_group) do |f| %>
  <% if @analysis_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@analysis_group.errors.count, "error") %> prohibited this analysis_group from being saved:</h2>

      <ul>
      <% @analysis_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
